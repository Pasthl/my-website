<!DOCTYPE html>
<html lang="<%= locals.lang === 'en' ? 'en' : 'zh' %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= locals.title || (locals.lang === 'en' ? 'Server Error' : '服务器错误') %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/"><%= locals.lang === 'en' ? 'About' : '关于我' %></a></li>
        <li><a href="/projects"><%= locals.lang === 'en' ? 'Projects' : '项目' %></a></li>
        <li><a href="/contact"><%= locals.lang === 'en' ? 'Contact' : '联系' %></a></li>
        <li class="lang-switch">
          <a href="/?lang=<%= locals.lang === 'en' ? 'zh' : 'en' %>">
            <%= locals.lang === 'en' ? '中文' : 'EN' %>
          </a>
        </li>
      </ul>
    </nav>
  </header>

  <main>
    <section class="error">
      <h1>500</h1>
      <h2><%= locals.lang === 'en' ? 'Server Error' : '服务器错误' %></h2>
      <p>
        <%= locals.lang === 'en' ? 
          'Sorry, something went wrong on our server. Please try again later.' : 
          '抱歉，服务器出现了问题。请稍后再试。' 
        %>
      </p>
      <a href="/" class="home-btn">
        <%= locals.lang === 'en' ? 'Go to Homepage' : '返回首页' %>
      </a>
    </section>
  </main>

  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Your Name. <%= locals.lang === 'en' ? 'All Rights Reserved.' : '版权所有。' %></p>
  </footer>
</body>
</html>